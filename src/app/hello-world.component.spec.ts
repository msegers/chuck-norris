import { HelloWorld } from "./hello-world.component";

describe("HelloWorldComponent", () => {
   it("can be constructed (this is a test test)", () => {
       expect(new HelloWorld()).not.toBe(undefined);


   })
});
